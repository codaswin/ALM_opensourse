# ============================================================
# SECRETS / CREDENTIALS — NEVER COMMIT
# ============================================================
.env
.env.*
!.env.example
*.env

# API keys / credentials
# NOTE: bare *credentials*/*secrets* substring globs used to live here and
# were removed — they matched ANY path containing that substring, including
# legitimate source files like app/memory/platform_credentials.py and
# app/safety/secrets.py (which hold no secrets themselves; the actual
# secrets are encrypted DB rows). That silently untracked 4 real source
# files and broke CI with ModuleNotFoundError on a fresh clone, since the
# files were never in the repo to begin with. The specific patterns below
# (credentials/, secrets/, credentials.json, etc.) already cover the real
# secret-file shapes this project has — no bare substring glob is needed.
*.pem
*.key
*.p12
*.pfx
*.crt
*.cer

# Local auth/token files
tokens/
credentials/
secrets/
.auth/
.auth.json
token.json
credentials.json


# ============================================================
# PYTHON
# ============================================================
.venv/
venv/
env/
ENV/

__pycache__/
*.py[cod]
*$py.class

.pytest_cache/
.mypy_cache/
.ruff_cache/
.pytype/
.pyre/
.coverage
.coverage.*
htmlcov/

*.egg-info/
.eggs/
build/
dist/
pip-log.txt
pip-delete-this-directory.txt

# Native sidecars are reproducible release artifacts, not source.
src-tauri/binaries/ai-linkedin-backend-*
src-tauri/target/
# Tauri regenerates its capability/ACL JSON schemas (IDE autocomplete only,
# not read at runtime) into this directory on every build.
src-tauri/gen/


# ============================================================
# DATABASES / LOCAL DATA
# ============================================================
*.db
*.sqlite
*.sqlite3

data/
storage/
uploads/
downloads/


# ============================================================
# AI / RAG / VECTOR DATA
# ============================================================
vectorstore/
vector_store/
faiss_index/
chroma/
.chroma/
embeddings/
/models/
checkpoints/

*.faiss
*.index
*.bin


# ============================================================
# AGENT MEMORY / RUNTIME STATE
# ============================================================
# Anchored to root — backend/app/memory/ is the real source package (CLAUDE.md
# project structure), not runtime state; an unanchored "memory/" here was
# silently hiding it from git (and backend/app/models/ via the models/ rule
# above) rather than the intended top-level runtime-artifact directory.
/memory/
runtime/
state/
sessions/
cache/
.cache/

logs/
*.log


# ============================================================
# IDE / EDITORS
# ============================================================
.vscode/
.idea/
*.swp
*.swo
*~

# Antigravity/local IDE data
.antigravity/

# Sublime Text
*.sublime-workspace
*.sublime-project

# VS Code "Local History" extension
.history/


# ============================================================
# OS FILES
# ============================================================
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes

Thumbs.db
ehthumbs.db
ehthumbs_vista.db
desktop.ini
$RECYCLE.BIN/


# ============================================================
# TEMP FILES
# ============================================================
tmp/
temp/
*.tmp
*.bak
*.orig
*.rej


# ============================================================
# TEST / COVERAGE ARTIFACTS
# ============================================================
.coverage
coverage.xml
htmlcov/
.tox/
.nox/


# ============================================================
# NODE / FRONTEND — for future frontend
# ============================================================
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

.next/
dist/
build/

# Vite/tsc incremental caches — normally under node_modules/ already, kept
# here defensively in case a tool's config points them elsewhere.
.vite/
*.tsbuildinfo
.eslintcache

.local


# ============================================================
# STRIX / PENTEST SCAN ARTIFACTS — local-only, not for GitHub
# ============================================================
# Strix (security-scan) run output, its scope/instruction file, and the
# app-data dir spun up for its live dynamic-validation target. None of
# these are useful to other contributors and some (scan scope, live app
# state/db) shouldn't be public.
/check/
/strix_runs/
/pentest-instruction.md
backend/.runtime-validation-appdata/
